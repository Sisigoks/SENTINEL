# Qwen3-32B-AWQ via vLLM — scale check (paper §6.2). Needs an A100/H100/B200-class GPU.
# `auto` serving fields resolved from the detected GPU (models/autotune.py).
kind: vllm
model_name: Qwen/Qwen3-32B-AWQ
quantization: null
max_model_len: auto
gpu_memory_utilization: auto
dtype: auto
tokenizer_mode: auto
trust_remote_code: false
seed: 0
