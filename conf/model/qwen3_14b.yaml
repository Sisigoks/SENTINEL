# Qwen3-14B-AWQ via vLLM. `auto` fields are resolved from the detected GPU (models/autotune.py):
# quantization auto-detected from config.json; max_model_len / gpu_memory_utilization scaled to VRAM.
kind: vllm
model_name: Qwen/Qwen3-14B-AWQ
quantization: null
max_model_len: auto
gpu_memory_utilization: auto
dtype: auto
tokenizer_mode: auto
trust_remote_code: false
seed: 0
