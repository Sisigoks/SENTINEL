# Mistral-Small via vLLM (HF-format repo; set tokenizer_mode=mistral for native-tokenizer repos).
# 24B in FP16 needs a big GPU; use an AWQ repo for smaller cards. `auto` resolved from the GPU.
kind: vllm
model_name: mistralai/Mistral-Small-Instruct-2409
quantization: null
max_model_len: auto
gpu_memory_utilization: auto
dtype: auto
tokenizer_mode: auto
trust_remote_code: false
seed: 0
