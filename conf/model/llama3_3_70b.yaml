# Llama-3.3-70B-Instruct (AWQ INT4) via vLLM — the frontier-scale check (paper §6.2).
# Completes the within-family scale axis against llama3_1_8b (8B -> 70B, same lineage).
# AWQ weights ~40GB -> needs an A100/H100-80GB (or H200/B200). Official FP16 repo:
# meta-llama/Llama-3.3-70B-Instruct (gated — accept the Meta license and set HF_TOKEN).
# `auto` serving fields resolved from the detected GPU (models/autotune.py).
kind: vllm
model_name: hugging-quants/Meta-Llama-3.3-70B-Instruct-AWQ-INT4
quantization: null
max_model_len: auto
gpu_memory_utilization: auto
dtype: auto
tokenizer_mode: auto
trust_remote_code: false
seed: 0
